{
  "name": "pretty-graph-monorepo",
  "private": true,
  "repository": {
    "type": "git",
    "url": "https://github.com/headbase/pretty-graph.git"
  },
  "license": "MIT",
  "scripts": {
    "bootstrap": "npm run clean:all && yarn install && lerna bootstrap",
    "build": "lerna run clean && lerna run build",
    "clean:all": "rimraf node_modules packages/*/node_modules",
    "tslint": "tslint -c tslint.json \"packages/**/*.ts\""
  },
  "devDependencies": {
    "jest": "^23.6.0",
    "lerna": "^3.8.1",
    "npm-run-all": "^4.1.5",
    "prettier": "^1.15.3",
    "rimraf": "^2.6.2",
    "tslint": "^5.12.0",
    "tslint-config-prettier": "^1.17.0",
    "typescript": "^3.2.2",
    "webpack": "4.28.3"
  },
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=8.9"
  }
}
